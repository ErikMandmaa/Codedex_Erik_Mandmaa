SELECT *
FROM shows
WHERE year > 2020;

# = equal to
# != not equal to
# > greater than
# < less than
# >= greater than or equal to
# <= less than or equal to

SELECT *
FROM shows
WHERE tomatometer < 60;